# Tests

Use this folder for pytest tests and small synthetic test data.

- Put pytest modules under `tests/`.
- Put tiny non-sensitive fixtures under `tests/data/`.
- Put local ad hoc outputs under `playground/` instead; that folder is ignored
  except for its `.gitignore`.
