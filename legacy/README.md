# Legacy Code

Historical notebooks and HPC modelling scripts live here for reference.

The active reusable benchmark implementation is in `ml_benchmark/`. MEPRAM
preprocessing and MEPRAM-specific configuration are in `mepram/`.
